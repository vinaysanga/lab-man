# Connection string for PostgreSQL in Docker environment
# Uses 'db' as hostname which is the service name in docker-compose
DATABASE_URL="postgres://postgres:14bM4n25@db:5432/lab-man"

# PostgreSQL configuration for Docker container
# These variables are used to initialize the database container
POSTGRES_USER=postgres
POSTGRES_PASSWORD=14bM4n25
POSTGRES_DB=lab-man